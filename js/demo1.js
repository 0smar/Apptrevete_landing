(function() {
	var triggerBttn = document.getElementById( 'trigger-overlay' ),
		overlay = document.querySelector( 'div.overlay' ),
		closeBttn = overlay.querySelector( 'button.overlay-close' );
		transEndEventNames = {
			'WebkitTransition': 'webkitTransitionEnd',
			'MozTransition': 'transitionend',
			'OTransition': 'oTransitionEnd',
			'msTransition': 'MSTransitionEnd',
			'transition': 'transitionend'
		},
		transEndEventName = transEndEventNames[ Modernizr.prefixed( 'transition' ) ],
		support = { transitions : Modernizr.csstransitions };

	function toggleOverlay() {
		if( classie.has( overlay, 'open' ) ) {
			classie.remove( overlay, 'open' );
			classie.add( overlay, 'close' );
			var onEndTransitionFn = function( ev ) {
				if( support.transitions ) {
					if( ev.propertyName !== 'visibility' ) return;
					this.removeEventListener( transEndEventName, onEndTransitionFn );
				}
				classie.remove( overlay, 'close' );
			};
			if( support.transitions ) {
				overlay.addEventListener( transEndEventName, onEndTransitionFn );
			}
			else {
				onEndTransitionFn();
			}
		}
		else if( !classie.has( overlay, 'close' ) ) {
			classie.add( overlay, 'open' );
		}
	}

	triggerBttn.addEventListener( 'click', toggleOverlay );
	closeBttn.addEventListener( 'click', toggleOverlay );

	var menuBttn = document.getElementById( 'menuButton' );
	menuBttn.addEventListener('click', toggleOverlay);

	var menuBttn2 = document.getElementById( 'menuButton2' );
	menuBttn2.addEventListener('click', toggleOverlay);

	var menuBttn3 = document.getElementById( 'menuButton3' );
	menuBttn3.addEventListener('click', toggleOverlay);

	var menuBttn4 = document.getElementById( 'menuButton4' );
	menuBttn4.addEventListener('click', toggleOverlay);

	var indexBttn = document.getElementById( 'homeButton' );
	indexBttn.addEventListener('click', toggleOverlay);
})();